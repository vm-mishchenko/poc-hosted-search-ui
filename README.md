## Notes
- user might want to specify few `search_queries`


- mapping
- pipeline
    - define list of facets to return
    - contains facets:
        - type
        - path
        - bucket's ranges
    - 