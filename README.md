ideas:
- autogenerate search index definition and query based on provided mongodb
  collection
- support autocomplete

limitations:
- does not support `$searchMeta` stage
    - but completely supports facets configured for `$search` stage
